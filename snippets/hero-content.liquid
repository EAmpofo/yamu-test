<div class="hero-content">
  <h1 class="hero-title">{{ title }}</h1>
  <p class="hero-description">{{ description }}</p>
  <a href="{{ button_link }}" class="hero-button">
    {{ button_text }}
  </a>
</div>
